hashmap的一些常见操作：
Map<String, Integer>map =new HashMap<>();
map.put("key",value);
map.get("key");
map.containsKey("key");
map.containsValue("value"); //
map.remove("key");
map.geto


//返回哈希表所有值
map.values();


hashSet一些常见操作：
Set<String>set = new HashSet<>();
set.add("value");
set.contains("value");
set.remove("value");
set.size();
set.isEmpty();
获取集合中某一个value：
for(String s:set){}

//算法统计神器数字
map.getOrDefault(key, defaultValue);
如：int count = map.getOrDefault(key, 0);


排序：
Arrays.sort(arr);
list.addAll(list);

list.add(Arrays.asList(nums[i], nums[j], nums[k]));



String s;
char[] chars = s.toCharArray();

    

比较函数
// 比较两个数，返回较大值
int max = Math.max(10, 20);      // 20

// 比较两个数，返回较小值
int min = Math.min(10, 20);      // 10














